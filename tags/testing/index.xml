<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>testing on Simon Aronsson</title>
    <link>https://simme.dev/tags/testing/</link>
    <description>Recent content in testing on Simon Aronsson</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-us</language>
    <managingEditor>simme@arcticbit.se (Simon Aronsson)</managingEditor>
    <webMaster>simme@arcticbit.se (Simon Aronsson)</webMaster>
    <lastBuildDate>Mon, 23 Aug 2021 00:00:00 +0000</lastBuildDate>
    
        <atom:link href="https://simme.dev/tags/testing/index.xml" rel="self" type="application/rss+xml" />
    
    
    <item>
      <title>Error Economics - How to avoid breaking the budget</title>
      <link>https://simme.dev/posts/error-economics/</link>
      <pubDate>Mon, 23 Aug 2021 00:00:00 +0000</pubDate>
      <author>simme@arcticbit.se (Simon Aronsson)</author>
      <guid>https://simme.dev/posts/error-economics/</guid>
      <description>&lt;p&gt;At &lt;a href=&#34;https://www.sloconf.com/&#34;&gt;SLOConf 2021&lt;/a&gt; I talked about how we may use error budgets to add pass/fail criterias to reliability tests we run as part of our CI pipelines.&lt;/p&gt;

&lt;div style=&#34;position: relative; padding-bottom: 56.25%; height: 0; overflow: hidden;&#34;&gt;
  &lt;iframe src=&#34;https://www.youtube.com/embed/9Z06PxppYOM&#34; style=&#34;position: absolute; top: 0; left: 0; width: 100%; height: 100%; border:0;&#34; allowfullscreen title=&#34;YouTube Video&#34;&gt;&lt;/iframe&gt;
&lt;/div&gt;


&lt;p&gt;As Site Reliability Engineers, one of our primary goals is to reduce manual labor, or toil, to a minimum while at the same time keeping the systems we manage as reliable and available as possible. To be able to do this in a safe way, it&amp;rsquo;s really important that we&amp;rsquo;re able to easily inspect the state of the system.&lt;/p&gt;

&lt;p&gt;To measure whether we&amp;rsquo;re successful in this endeavour, we establish service level agreements (SLA), service level indicators (SLI) and service level objectives (SLO). Traditional monitoring is really helpful in doing this, but it won&amp;rsquo;t allow you to take action until the issue is already present, likely already affecting your users, in prod.&lt;/p&gt;

&lt;p&gt;To be able to take action proactively, we may use something like a load generator or reliability testing tool to simulate load in our system, measuring how it behaves even before we&amp;rsquo;ve released anything in production. While we&amp;rsquo;ll never will be able to compensate fully for the fact that it won&amp;rsquo;t be running in production, we can still simulate production-like load, possibly even while injecting real-world turbulence into the system, giving us a pretty good picture of what we can expect in production as well.&lt;/p&gt;

&lt;h2 id=&#34;how-do-we-measure-success-for-site-reliability&#34;&gt;How do we measure success for site reliability?&lt;/h2&gt;

&lt;p&gt;When we start out creating these service level artifacts, it&amp;rsquo;s usually tempting to over-engineer them, trying to take every edge case into account. My recommendation is that you try to avoid this to the extent possible.&lt;/p&gt;

&lt;p&gt;Instead, aim for a simple set of indicators and objectives, that are generic enough so that you may use them for multiple systems. You may then expand on them and make them more specific as your understanding of the systems you manage increase over time. Doing this is likely to save you a lot of time, as we otherwise tend to come up with unrealistic or irrelevant measurements or requirements, mainly due to our lack of experience.&lt;/p&gt;

&lt;h2 id=&#34;service-level-indicators&#34;&gt;Service Level Indicators&lt;/h2&gt;

&lt;p&gt;Service level indicators are quantitative measures of a system&amp;rsquo;s health. To make it easy to remember, we may think of this as what we are measuring. If we, for instance, try to come up with some SLIs for a typical web application, we are likely to end up with things like request duration, uptime, and error rates.&lt;/p&gt;

&lt;h2 id=&#34;creating-slis-and-slos&#34;&gt;Creating SLIs and SLOs&lt;/h2&gt;

&lt;p&gt;What should be included? In most cases, we only want to include valid requests. A good formula to follow when crafting SLIs is available in the &lt;a href=&#34;https://sre.google/&#34;&gt;Google SRE docs&lt;/a&gt;. Those state that an SLI equals the amount of good events, divided by the amount of valid events, times a hundred, expressed as percentages.&lt;/p&gt;

&lt;p&gt;As an example: if a user decides to send us a request that is not within the defined constraints of the service, we should of course handle it gracefully, letting the user know the request is unsupported. However, we shouldn&amp;rsquo;t be held responsible for the request not being processed properly.&lt;/p&gt;

&lt;h2 id=&#34;service-level-objectives&#34;&gt;Service Level Objectives&lt;/h2&gt;

&lt;p&gt;Service level objectives on the other hand, are the targets we set for our SLIs. Think of it as what the measures should show to be OK. For instance, if our SLI is based on request duration, and shows how many percent of all requests are below 500ms, our SLO would express how big a percentage we expect to be below 500ms for our service to be considered to be within the limits.&lt;/p&gt;

&lt;h2 id=&#34;what-is-an-error-budget&#34;&gt;What is an error budget?&lt;/h2&gt;

&lt;p&gt;An error budget is the remainder of the SLI once the SLO has been applied. For instance, if our SLO is 99.9%, that would mean our error budget is the remaining .1% up to a 100%. To not breach our SLOs, we then need to be able to fit all events that would not adhere to the criteria we set up into that .1%. This includes outages, service degradations and even planned maintenance windows.&lt;/p&gt;

&lt;p&gt;What I&amp;rsquo;m trying to say is that while it might feel tempting to go for four nines, or even three as your SLO (99.99%, 99,9%), this has astronomic impact on the engineering effort needed. For a downtime/unavailability SLI, a three nine SLO basically means that you can afford as little as:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;Daily: 1m 26s&lt;/li&gt;
&lt;li&gt;Weekly: 10m 4s&lt;/li&gt;
&lt;li&gt;Monthly: 43m 49s&lt;/li&gt;
&lt;li&gt;Quarterly: 2h 11m 29s&lt;/li&gt;
&lt;li&gt;Yearly: 8h 45m 56s&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;For comparing &amp;ldquo;nines&amp;rdquo;, navigate to &lt;a href=&#34;https://uptime.is&#34;&gt;uptime.is&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;In my experience, very few systems are critical enough to motivate this level of availability. With an SLO like this, even with rolling restarts and zero downtime deploys, we can&amp;rsquo;t really afford to make any mistakes at all.&lt;/p&gt;

&lt;h2 id=&#34;burning-the-budget&#34;&gt;Burning the budget&lt;/h2&gt;

&lt;p&gt;When would it be acceptable to burn the budget on purpose? I like to use the following sentence as a rule of thumb:&lt;/p&gt;

&lt;blockquote&gt;
&lt;p&gt;It is only acceptable to burn error budget on purpose if the goal of the activity causing the burn is to reduce the burn-rate going forward.&lt;/p&gt;
&lt;/blockquote&gt;

&lt;h2 id=&#34;setting-expectations&#34;&gt;Setting expectations&lt;/h2&gt;

&lt;h3 id=&#34;picking-our-slis&#34;&gt;Picking our SLIs&lt;/h3&gt;

&lt;p&gt;In this demo, we&amp;rsquo;ll be testing a made-up online food ordering service called Hipster Pizza. As service level indicators, we&amp;rsquo;ll be using the response time of requests and the HTTP response status success rate.&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;https://simme.dev/blog/static/b61c2d22b2b553d2a050bfefec97a066/37e03/hipster-pizza.jpg&#34;&gt;&lt;img src=&#34;https://k6.io/blog/static/b61c2d22b2b553d2a050bfefec97a066/37e03/hipster-pizza.jpg&#34; alt=&#34;hipster pizza&#34; title=&#34;hipster pizza&#34; /&gt;&lt;/a&gt;&lt;/p&gt;

&lt;h3 id=&#34;picking-our-slos&#34;&gt;Picking our SLOs&lt;/h3&gt;

&lt;p&gt;What would be reasonable SLOs for these SLIs? First we got to ask ourselves if we already have commitments to our customers or users in the form of SLAs. If we do, we at the very least need to stay within that.&lt;/p&gt;

&lt;p&gt;However, it&amp;rsquo;s also good to agree on our internal ambitions. And usually, these ambitions turn out to be far less forgiving than whatever we dare to promise our users.&lt;/p&gt;

&lt;p&gt;In this example, we&amp;rsquo;ll use the following SLOs:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;95% of all valid requests will have a response time below 300ms&lt;/li&gt;
&lt;li&gt;99.9% of all valid requests will reply with a successful HTTP Response status.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;This means that the error budget for response time is 5%, while the error budget for HTTP success is 0.1%.&lt;/p&gt;

&lt;h2 id=&#34;measuring&#34;&gt;Measuring&lt;/h2&gt;

&lt;p&gt;To know whether we are able to stay within budget, we need to measure this in production. And we also need to assign a time window to our SLOs. For instance, that the SLO is measured on a month-to-month basis, or a sliding 7-day window.&lt;/p&gt;

&lt;p&gt;We also need to test this somehow continuously to make sure whether a certain change introduces regression, preventing us from hitting our target. This is where k6, or load generators in general, come in.&lt;/p&gt;

&lt;p&gt;Most of the time we only speak about monitoring our SLOs. I would like to propose that we take this one step further. With the traditional approach of monitoring, we&amp;rsquo;re not really equipped to react prior to consuming the budget, especially with the extremely tight budgets we had a look at earlier. Instead we&amp;rsquo;re only going to be alerted once we&amp;rsquo;re already approaching SLO game over.&lt;/p&gt;

&lt;p&gt;Don&amp;rsquo;t get me wrong here, I still believe we need, and should, monitor our production SLOs, but we should also complement this with some kind of indicative testing, allowing us to take action before the budget breach has occured. Possibly even stopping the release altogether until the issue has been resolved.&lt;/p&gt;

&lt;p&gt;By running a test that simulates the traffic and behavior of users in production, we&amp;rsquo;re able to extrapolate the effect a change would have over time and use that as an indicator of how the change would affect production SLOs.&lt;/p&gt;

&lt;p&gt;Before we get into that, however, we also need to talk a bit about scheduled downtime, or maintenance windows.&lt;/p&gt;

&lt;h2 id=&#34;accounting-for-scheduled-downtime&#34;&gt;Accounting for scheduled downtime&lt;/h2&gt;

&lt;p&gt;In a real-world production system, these likely occur all the time. In some cases, this is possible without requiring any downtime whatsoever, but for the vast majority, some downtime every now and then is unavoidable, even with rolling restarts, canaries, feature flags and red-green deployments in place.&lt;/p&gt;

&lt;p&gt;We should put some time into identifying what activities we do that actually require downtime, and account for that in our test. If our SLOs are measured on a month to month basis, and we usually have 10 minutes of downtime every workday, we also need to deduct a corresponding amount from our error budget.&lt;/p&gt;

&lt;p&gt;For a month with 31 one days, 22 of them being workdays, 10 minutes of downtime every workday would mean we have a planned downtime of 220 minutes per 744 hours, or 0.0049%.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;220/(744*60) = 0,0049%

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;We&amp;rsquo;ll now adjust the SLOs we use in our test accordingly, prior to calculating the error budget. Heavily simplified, not taking usage volume spread and such into account, this would in our case mean the actual error budgets for our test would be 0,0951% and 4,9951%.&lt;/p&gt;

&lt;h2 id=&#34;demo&#34;&gt;Demo&lt;/h2&gt;

&lt;p&gt;By using these calculated error budgets, we may then express them as thresholds in our tests, and use them as pass/fail criteria for whether our build was successful or not. And once we have those in our CI workflow, we&amp;rsquo;ll also be able to increase our confidence in product iterations not breaking the error budget.&lt;/p&gt;

&lt;p&gt;Let&amp;rsquo;s have a look at how this could look in k6. k6 is available for free and as open-source. Hooking it up with your pre-existing CI pipelines is usually done without any additional cost or significant time investment.&lt;/p&gt;

&lt;p&gt;If you&amp;rsquo;re using some other load testing tool that also support setting runtime thresholds, this will likely work just as well there. For this demo, we&amp;rsquo;re gonna use this small test script.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;import http from &#39;k6/http&#39;

export const options = {
  vus: 60,
  duration: &#39;30s&#39;
}

export default function() {
  const res = http.get(&#39;https://test-api.k6.io&#39;)
}

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;So what does this script actually do? For a duration of 30 seconds, it will run 50 virtual users in parallel, all visiting the page &lt;a href=&#34;https://test-api.k6.io&#34;&gt;https://test-api.k6.io&lt;/a&gt; as many times as possible. In a real world scenario, this test would most likely be a lot more extensive, and try to mimic a user&amp;rsquo;s interaction with the service we&amp;rsquo;re defining our SLO for.&lt;/p&gt;

&lt;p&gt;Let&amp;rsquo;s run our test and have a look at the stats it returns:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;http_req_duration..............: avg=132.05ms min=101.44ms med=127.55ms max=284.2ms p(90)=156.19ms p(95)=165.75ms
http_req_failed................: 0.00% ✓ 0 ✗ 6576

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;As you can see, we already get all the information we need to be able to make out whether we fulfil our SLOs. Let&amp;rsquo;s also define some thresholds to automatically detect whether our test busted our error budget or not.&lt;/p&gt;

&lt;p&gt;Let&amp;rsquo;s set those as our thresholds in our k6 script.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;  export const options = {
    thresholds: {
      http_req_duration: [&#39;p(95.0049)&amp;lt;300&#39;], // 95% below 300ms, accounting for planned downtime
      http_req_failed: [&#39;rate&amp;lt;0.00951&#39;], // 99,99049% successful, accounting for planned downtime
    }
  }

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;That&amp;rsquo;s it! By using your SLOs and SLIs as pass/fail thresholds in your CI workflow you&amp;rsquo;ll be able to increase your confidence in product iterations not breaking the error budget.&lt;/p&gt;</description>
    </item>
    
    <item>
      <title>Running distributed k6 tests on Kubernetes</title>
      <link>https://simme.dev/posts/running-distributed-k6-tests-on-kubernetes/</link>
      <pubDate>Thu, 11 Feb 2021 00:00:00 +0100</pubDate>
      <author>simme@arcticbit.se (Simon Aronsson)</author>
      <guid>https://simme.dev/posts/running-distributed-k6-tests-on-kubernetes/</guid>
      <description>&lt;p&gt;One of the questions we often get in the forum is how to run distributed k6 tests on your own infrastructure. While we believe that &lt;a href=&#34;https://k6.io/docs/testing-guides/running-large-tests&#34;&gt;running large load tests&lt;/a&gt; is possible even when running on a single node, we do appreciate that this is something some of our users might want to do.&lt;/p&gt;

&lt;blockquote&gt;
&lt;h3 id=&#34;what-you-will-learn&#34;&gt;📖What you will learn&lt;/h3&gt;

&lt;ul&gt;
&lt;li&gt;What the operator pattern is and when it is useful&lt;/li&gt;
&lt;li&gt;Deploying the k6 operator in your kubernetes cluster&lt;/li&gt;
&lt;li&gt;Running a distributed k6 test in your own cluster&lt;/li&gt;
&lt;/ul&gt;

&lt;h4 id=&#34;experimental&#34;&gt;⚠️ Experimental&lt;/h4&gt;

&lt;p&gt;The project used in this article is experimental and changes a lot between commits. Use at your own discretion .&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;&lt;a href=&#34;https://simme.dev/blog/static/49d58b70df40a0fa1aa75dd1f6d1f670/acdd1/operator.png&#34;&gt;&lt;img src=&#34;https://k6.io/blog/static/49d58b70df40a0fa1aa75dd1f6d1f670/7842b/operator.png&#34; alt=&#34;operator&#34; title=&#34;operator&#34; /&gt;&lt;/a&gt;&lt;/p&gt;

&lt;h2 id=&#34;introduction&#34;&gt;Introduction&lt;/h2&gt;

&lt;p&gt;There are at least a couple of reasons why you would want to do this:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;p&gt;You run everything else in Kubernetes and would like k6 to be executed in the same fashion as all your other infrastructure components.&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;You have access to a couple of high-end nodes and want to pool their resources into a large-scale stress test.&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;You have access to multiple low-end or highly utilized nodes and need to pool their resources to be able to reach your target VU count or Requests per Second (RPS).&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&#34;prerequisites&#34;&gt;Prerequisites&lt;/h2&gt;

&lt;p&gt;To be able to follow along in this guide, you’ll need access to a Kubernetes cluster, with enough privileges to apply objects.&lt;/p&gt;

&lt;p&gt;You’ll also need:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/kubernetes-sigs/kustomize/&#34;&gt;Kustomize&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://kubernetes.io/docs/tasks/tools/install-kubectl/&#34;&gt;Kubectl&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://www.gnu.org/software/make/&#34;&gt;Make&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&#34;the-kubernetes-operator-pattern&#34;&gt;The Kubernetes Operator pattern&lt;/h2&gt;

&lt;p&gt;The &lt;a href=&#34;https://kubernetes.io/docs/concepts/extend-kubernetes/operator/&#34;&gt;operator pattern&lt;/a&gt; is a way of extending Kubernetes so that you may use &lt;a href=&#34;https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/custom-resources/&#34;&gt;custom resources&lt;/a&gt; to manage applications running in the cluster. The pattern aims to automate the tasks that a human operator would usually do, like provisioning new application components, changing the configuration, or resolving problems that occur.&lt;/p&gt;

&lt;p&gt;This is accomplished using custom resources which, for the scope of this article, could be compared to the traditional service requests that you would file to your system operator to get changes applied to the environment.&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;https://simme.dev/blog/static/8bc25b5fb3de365092d17de6121c3280/d9c41/pattern.png&#34;&gt;&lt;img src=&#34;https://k6.io/blog/static/8bc25b5fb3de365092d17de6121c3280/7842b/pattern.png&#34; alt=&#34;operator pattern&#34; title=&#34;operator pattern&#34; /&gt;&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;The operator will listen for changes to, or creation of, K6 custom resource objects. Once a change is detected, it will react by modifying the cluster state, spinning up k6 test jobs as needed. It will then use the parallelism argument to figure out how to split the workload between the jobs using &lt;a href=&#34;https://k6.io/docs/using-k6/options#execution-segment&#34;&gt;execution segments&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&#34;using-the-k6-operator-to-run-a-distributed-load-test-in-your-kubernetes-cluster&#34;&gt;Using the k6 operator to run a distributed load test in your Kubernetes cluster&lt;/h2&gt;

&lt;p&gt;We&amp;rsquo;ll now go through the steps required to deploy, run, and clean up after the k6 operator.&lt;/p&gt;

&lt;h3 id=&#34;cloning-the-repository&#34;&gt;Cloning the repository&lt;/h3&gt;

&lt;p&gt;Before we get started, we need to clone the operator repository from GitHub and navigate to the repository root:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ git clone https://github.com/k6io/operator &amp;amp;&amp;amp; cd operator

&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;deploying-the-operator&#34;&gt;Deploying the operator&lt;/h3&gt;

&lt;p&gt;Deploying the operator is done by running the command below, with kubectl configured to use the context of the cluster that you want to deploy it to.&lt;/p&gt;

&lt;p&gt;First, make sure you are using the right context:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ kubectl config get-contexts

CURRENT NAME CLUSTER AUTHINFO NAMESPACE
* harley harley harley
          jean jean jean
          ripley ripley ripley

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Then deploy the operator bundle using make. This will also apply the roles, namespaces, bindings and services needed to run the operator.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ make deploy

/Users/simme/.go/bin/controller-gen &amp;quot;crd:trivialVersions=true&amp;quot; rbac:roleName=manager-role webhook paths=&amp;quot;./...&amp;quot; output:crd:artifacts:config=config/crd/bases
cd config/manager &amp;amp;&amp;amp; /Users/simme/.go/bin/kustomize edit set image controller=ghcr.io/k6io/operator:latest
/Users/simme/.go/bin/kustomize build config/default | kubectl apply -f -
namespace/k6-operator-system created
customresourcedefinition.apiextensions.k8s.io/k6s.k6.io created
role.rbac.authorization.k8s.io/k6-operator-leader-election-role created
clusterrole.rbac.authorization.k8s.io/k6-operator-manager-role created
clusterrole.rbac.authorization.k8s.io/k6-operator-proxy-role created
clusterrole.rbac.authorization.k8s.io/k6-operator-metrics-reader created
rolebinding.rbac.authorization.k8s.io/k6-operator-leader-election-rolebinding created
clusterrolebinding.rbac.authorization.k8s.io/k6-operator-manager-rolebinding created
clusterrolebinding.rbac.authorization.k8s.io/k6-operator-proxy-rolebinding created
service/k6-operator-controller-manager-metrics-service created
deployment.apps/k6-operator-controller-manager created

&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;writing-our-test-script&#34;&gt;Writing our test script&lt;/h3&gt;

&lt;p&gt;Once that is done, we need to create a config map containing the test script. For the operator to pick up our script, we need to name the file &lt;code&gt;test.js&lt;/code&gt;. For this article, we’ll be using the test script below:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;import http from &#39;k6/http&#39;;
import { check } from &#39;k6&#39;;

export let options = {
  stages: [
    { target: 200, duration: &#39;30s&#39; },
    { target: 0, duration: &#39;30s&#39; },
  ],
};

export default function () {
  const result = http.get(&#39;https://test-api.k6.io/public/crocodiles/&#39;);
  check(result, {
    &#39;http response status code is 200&#39;: result.status === 200,
  });
}

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Before we continue, we&amp;rsquo;ll run the script once locally to make sure it works:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ k6 run test.js

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If you’ve never written a k6 test before, we recommend that you start by reading &lt;a href=&#34;https://k6.io/docs/getting-started/running-k6&#34;&gt;this getting started article from the documentation&lt;/a&gt;, just to get a feel for how it works.&lt;/p&gt;

&lt;p&gt;Let’s walk through this script and make sure we understand what is happening: We’ve set up two stages that will run for 30 seconds each. The first one will ramp up to linearly to 200 VUs over 30 seconds. The second one will ramp down to 0 again over 30 seconds.&lt;/p&gt;

&lt;p&gt;In this case the operator will tell each test runner to run only a portion of the total VUs. For instance, if the script calls for 40 VUs, and &lt;code&gt;parallelism&lt;/code&gt; is set to 4, the test runners would have 10 VUs each.&lt;/p&gt;

&lt;p&gt;Each VU will then loop over the default function as many times as possible during the execution. It will execute an HTTP GET request against the URL we’ve configured, and make sure that the responds with HTTP Status 200. In a real test, we&amp;rsquo;d probably throw in a sleep here to emulate the think time of the user, but as the purpose of this article is to run a distributed test with as much throughput as possible, I&amp;rsquo;ve deliberately skipped it.&lt;/p&gt;

&lt;h3 id=&#34;deploying-our-test-script&#34;&gt;Deploying our test script&lt;/h3&gt;

&lt;p&gt;Once the test script is done, we have to deploy it to the kubernetes cluster. We’ll use a &lt;code&gt;ConfigMap&lt;/code&gt; to accomplish this. The name of the map can be whatever you like, but for this demo we&amp;rsquo;ll go with &lt;code&gt;crocodile-stress-test&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;If you want more than one test script available in your cluster, you just repeat this process for each one, giving the maps different names.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ kubectl create configmap crocodile-stress-test --from-file /path/to/our/test.js

configmap/crocodile-stress-test created

&lt;/code&gt;&lt;/pre&gt;

&lt;blockquote&gt;
&lt;h4 id=&#34;namespaces&#34;&gt;⚠️ Namespaces&lt;/h4&gt;

&lt;p&gt;For this to work, the k6 custom resource and the config map needs to be deployed in the same namespace.&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;Let’s have a look at the result:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ kubectl describe configmap crocodile-stress-test

Name: crocodile-stress-test
Namespace: default
Labels: &amp;lt;none&amp;gt;
Annotations: &amp;lt;none&amp;gt;

Data
====
test.js:
----
import http from &#39;k6/http&#39;;
import { check } from &#39;k6&#39;;

export let options = {
  stages: [
    { target: 200, duration: &#39;30s&#39; },
    { target: 0, duration: &#39;30s&#39; },
  ],
};

export default function () {
  const result = http.get(&#39;https://test-api.k6.io/public/crocodiles/&#39;);
  check(result, {
    &#39;http response status code is 200&#39;: result.status === 200,
  });
}

Events: &amp;lt;none&amp;gt;

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The config map contains the content of our test file, labelled as test.js. The operator will later search through our config map for this key, and use its content as the test script.&lt;/p&gt;

&lt;h3 id=&#34;creating-our-custom-resource-cr&#34;&gt;Creating our custom resource (CR)&lt;/h3&gt;

&lt;p&gt;To communicate with the operator, we’ll use a custom resource called &lt;code&gt;K6&lt;/code&gt;. Custom resources behave just as native kubernetes objects, while being fully customizable. In this case, the data of the custom resource contains all the information necessary for k6 operator to be able to start a distributed load test:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;apiVersion: k6.io/v1alpha1
kind: K6
metadata:
  name: k6-sample
spec:
  parallelism: 4
  script: crocodile-stress-test

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;For Kubernetes to know what to do with this custom resource, we first need to specify what API Version we want to use to interpret its content, in this case &lt;code&gt;k6.io/v1alpha1&lt;/code&gt;. We’ll then set the kind to K6, and give our resource a name.&lt;/p&gt;

&lt;p&gt;As the specification for our custom resource, we now have the option to use a couple of different properties:&lt;/p&gt;

&lt;h4 id=&#34;parallelism&#34;&gt;Parallelism&lt;/h4&gt;

&lt;p&gt;Configures how many k6 test runner jobs the operator should spawn.&lt;/p&gt;

&lt;h4 id=&#34;script&#34;&gt;Script&lt;/h4&gt;

&lt;p&gt;The name of the config map containing our &lt;code&gt;script.js&lt;/code&gt; file.&lt;/p&gt;

&lt;h4 id=&#34;separate&#34;&gt;Separate&lt;/h4&gt;

&lt;p&gt;Whether the operator should allow multiple k6 jobs to run concurrently at the same node. The default value for this property is &lt;code&gt;false&lt;/code&gt;, allowing each node to run multiple jobs.&lt;/p&gt;

&lt;h4 id=&#34;arguments&#34;&gt;Arguments&lt;/h4&gt;

&lt;p&gt;Allowing you to pass arguments to each k6 job, just as you would from the CLI. For instance &lt;code&gt;--tag testId=crocodile-stress-test-1&lt;/code&gt;,&lt;code&gt;--out cloud&lt;/code&gt;, or &lt;code&gt;—no-connection-reuse&lt;/code&gt;.&lt;/p&gt;

&lt;h3 id=&#34;deploying-our-custom-resource&#34;&gt;Deploying our Custom Resource&lt;/h3&gt;

&lt;p&gt;We will now deploy our custom resource using kubectl, and by that, start the test:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ kubectl apply -f /path/to/our/k6/custom-resource.yml

k6.k6.io/k6-sample created

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Once we do this, the k6 operator will pick up the changes and start the execution of the test. This looks somewhat along the lines of what is shown in this diagram:&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;https://simme.dev/blog/static/8c12a4c120f2f4feed3d7284df4be089/14945/pattern-k6.png&#34;&gt;&lt;img src=&#34;https://k6.io/blog/static/8c12a4c120f2f4feed3d7284df4be089/14945/pattern-k6.png&#34; alt=&#34;k6 pattern&#34; title=&#34;k6 pattern&#34; /&gt;&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;Let’s make sure everything went as expected:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ kubectl get k6 

NAME AGE
k6-sample 2s

$ kubectl get jobs

NAME COMPLETIONS DURATION AGE
k6-sample-1 0/1 12s 12s
k6-sample-2 0/1 12s 12s
k6-sample-3 0/1 12s 12s
k6-sample-4 0/1 12s 12s

$ kubectl get pods
NAME READY STATUS RESTARTS AGE
k6-sample-3-s7hdk 1/1 Running 0 20s
k6-sample-4-thnpw 1/1 Running 0 20s
k6-sample-2-f9bbj 1/1 Running 0 20s
k6-sample-1-f7ktl 1/1 Running 0 20s

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The pods have now been created and put in a paused state until the operator has made sure they’re all ready to execute the test. Once that’s the case, the operator deploys another job, k6-sample-starter which is responsible for making sure all our runners start execution at the same time.&lt;/p&gt;

&lt;p&gt;Let’s wait a couple of seconds and then list our pods again:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ kubectl get pods

NAME READY STATUS RESTARTS AGE
k6-sample-3-s7hdk 1/1 Running 0 76s
k6-sample-4-thnpw 1/1 Running 0 76s
k6-sample-2-f9bbj 1/1 Running 0 76s
k6-sample-1-f7ktl 1/1 Running 0 76s
k6-sample-starter-scw59 0/1 Completed 0 56s

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;All right! The starter has completed and our tests are hopefully running. To make sure, we may check the logs of one of the jobs:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ kubectl logs k6-sample-1-f7ktl

[...]

Run [100%] paused
default [0%]

Run [100%] paused
default [0%]

running (0m00.7s), 02/50 VUs, 0 complete and 0 interrupted iterations
default [1%] 02/50 VUs 0m00.7s/1m00.0s

running (0m01.7s), 03/50 VUs, 13 complete and 0 interrupted iterations
default [3%] 03/50 VUs 0m01.7s/1m00.0s

running (0m02.7s), 05/50 VUs, 41 complete and 0 interrupted iterations
default [4%] 05/50 VUs 0m02.7s/1m00.0s

[...]

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;And with that, our test is running! 🎉 After a couple of minutes, we’re now able to list the jobs again to verify they’ve all completed:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ kubectl get jobs

NAME COMPLETIONS DURATION AGE
k6-sample-starter 1/1 8s 6m2s
k6-sample-3 1/1 96s 6m22s
k6-sample-2 1/1 96s 6m22s
k6-sample-1 1/1 97s 6m22s
k6-sample-4 1/1 97s 6m22s

&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;cleaning-up&#34;&gt;Cleaning up&lt;/h3&gt;

&lt;p&gt;To clean up after a test run, we delete all resources using the same yaml file we used to deploy it:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ kubectl delete -f /path/to/our/k6/custom-resource.yml

k6.k6.io &amp;quot;k6-sample&amp;quot; deleted

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Which deletes all the resources created by the operator as well, as shown below:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ kubectl get jobs
No resources found in default namespace.

$ kubectl get pods
No resources found in default namespace.

&lt;/code&gt;&lt;/pre&gt;

&lt;blockquote&gt;
&lt;h4 id=&#34;deleting-the-operator&#34;&gt;⚠️ Deleting the operator&lt;/h4&gt;

&lt;p&gt;If you for some reason would like to delete the operator altogether, just run make delete from the root of the project..&lt;/p&gt;

&lt;p&gt;The idea behind the operator however, is that you let it remain in your cluster between test executions, only applying and deleting the actual K6 custom resources used to run the tests.&lt;/p&gt;
&lt;/blockquote&gt;

&lt;h2 id=&#34;things-to-consider&#34;&gt;Things to consider&lt;/h2&gt;

&lt;p&gt;While the operator makes running distributed load tests a lot easier, it still comes with a couple of drawbacks or gotchas that you need to be aware of and plan for. For instance, the lack of metric aggregation.&lt;/p&gt;

&lt;p&gt;We’ll go through in detail how to set up the monitoring and visualisation of these test runs in a future article, but for now, here’s a list of things you might want to consider:&lt;/p&gt;

&lt;h3 id=&#34;metrics-will-not-be-automatically-aggregated&#34;&gt;Metrics will not be automatically aggregated&lt;/h3&gt;

&lt;p&gt;Metrics generated by running distributed k6 tests using the operator won’t be aggregated, which means that each test runner will produce its own results and end-of-test summary.&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;To be able to aggregate your metrics and analyse them together, you’ll either need to:&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;1) Set up some kind of monitoring or visualisation software and configure your K6 custom resource to make your jobs output there.&lt;/p&gt;

&lt;p&gt;2) Use &lt;a href=&#34;https://github.com/elastic/logstash&#34;&gt;logstash&lt;/a&gt;, &lt;a href=&#34;https://github.com/fluent/fluentd&#34;&gt;fluentd&lt;/a&gt;, splunk, or similar to parse and aggregate the logs yourself.&lt;/p&gt;

&lt;h3 id=&#34;thresholds-are-not-evaluated-across-jobs-at-runtime&#34;&gt;Thresholds are not evaluated across jobs at runtime&lt;/h3&gt;

&lt;p&gt;As the metrics are not aggregated at runtime, your thresholds won’t be evaluated using aggregation either. Currently, the best way to solve this is by setting up alarms for passed thresholds in your monitoring or visualisation software instead.&lt;/p&gt;

&lt;h3 id=&#34;overpopulated-nodes-might-create-bottlenecks&#34;&gt;Overpopulated nodes might create bottlenecks&lt;/h3&gt;

&lt;p&gt;You want to make sure your k6 jobs have enough cpu and memory resources to actually perform your test. Using parallelism alone might not be sufficient. If you run into this issue, experiment with using the separate property.&lt;/p&gt;

&lt;h3 id=&#34;experimental-1&#34;&gt;Experimental&lt;/h3&gt;

&lt;p&gt;As mentioned in the beginning of the article, the operator &lt;em&gt;is&lt;/em&gt; experimental, and as such it might change a lot from commit to commit.&lt;/p&gt;

&lt;h3 id=&#34;total-cost-of-ownership&#34;&gt;Total cost of ownership&lt;/h3&gt;

&lt;p&gt;The k6 operator significantly simplifies the process of running distributed load tests in your own cluster. However, there still is a maintenance burden associated with self-hosting. If you&amp;rsquo;d rather skip that, as well as the other drawbacks listed above, and instead get straight to load testing, you might want to have a look at the &lt;a href=&#34;https://k6.io/cloud&#34;&gt;k6 cloud offering&lt;/a&gt;.&lt;/p&gt;

&lt;h2 id=&#34;see-also&#34;&gt;See also&lt;/h2&gt;

&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/k6io/operator&#34;&gt;The k6 operator project on GitHub&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;</description>
    </item>
    
    <item>
      <title>Performance testing gRPC services</title>
      <link>https://simme.dev/posts/performance-testing-grpc-services/</link>
      <pubDate>Thu, 12 Nov 2020 00:00:00 +0100</pubDate>
      <author>simme@arcticbit.se (Simon Aronsson)</author>
      <guid>https://simme.dev/posts/performance-testing-grpc-services/</guid>
      <description>&lt;p&gt;In this article, we&amp;rsquo;ll go through how to use k6 to performance test your gRPC services.&lt;/p&gt;

&lt;blockquote&gt;
&lt;h3 id=&#34;what-you-will-learn&#34;&gt;📖 What you will learn&lt;/h3&gt;

&lt;ul&gt;
&lt;li&gt;What gRPC is.&lt;/li&gt;
&lt;li&gt;How gRPC differs from JSON-based REST.&lt;/li&gt;
&lt;li&gt;Creating and executing your first gRPC performance test using k6.&lt;/li&gt;
&lt;/ul&gt;
&lt;/blockquote&gt;

&lt;h2 id=&#34;outline&#34;&gt;Outline&lt;/h2&gt;

&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;#what-is-grpc&#34;&gt;What is gRPC&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;#api-types&#34;&gt;API Types&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;#the-proto-definition&#34;&gt;The proto definition&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;#getting-started&#34;&gt;Getting started&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;#summary&#34;&gt;Summary&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;#see-also&#34;&gt;See also&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&#34;what-is-grpc&#34;&gt;What is gRPC&lt;/h2&gt;

&lt;p&gt;gRPC is a light-weight open-source RPC framework. It was originally developed by Google, with 1.0 being released in August 2016. Since then, it&amp;rsquo;s gained a lot of attention as well as a wide adoption.&lt;/p&gt;

&lt;p&gt;In comparison to JSON, which is transmitted as human-readable text, gRPC is binary, making it both faster to transmit and more compact. In the benchmarks we&amp;rsquo;ve seen between gRPC and JSON-based REST, gRPC has proved to be a lot faster than its more traditional counterpart.&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;https://auth0.com/blog/beating-json-performance-with-protobuf/&#34;&gt;A benchmark by Auth0&lt;/a&gt; reported up to 6 times higher performance, while other benchmarks, like&lt;a href=&#34;https://dev.to/plutov/benchmarking-grpc-and-rest-in-go-565&#34;&gt;this one by Alex Pliutau&lt;/a&gt; or&lt;a href=&#34;https://medium.com/@EmperorRXF/evaluating-performance-of-rest-vs-grpc-1b8bdf0b22da&#34;&gt;this one by Ruwan Fernando&lt;/a&gt;, suggests improvements of up to 10 times.&lt;/p&gt;

&lt;p&gt;For chatty, distributed systems, these improvements accumulate quickly, making the difference not only noticeable in benchmarks, but also by the end-user.&lt;/p&gt;

&lt;h2 id=&#34;api-types&#34;&gt;API types&lt;/h2&gt;

&lt;p&gt;gRPC supports four different types of RPCs, unary, server streaming, client streaming, and bi-directional streaming. In reality, the messages are multiplexed using the same connection, but in the spirit of keeping things simple and approachable, this is not illustrated in the gRPC service model diagrams below.&lt;/p&gt;

&lt;h3 id=&#34;unary&#34;&gt;Unary&lt;/h3&gt;

&lt;p&gt;Unary calls work the same way as a regular function call: a single request is sent to the server which in turn replies with a single response.&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;https://simme.dev/blog/static/c48fc5ca8336d9cd8b98618c4b9d86ec/7842b/grpc-unary.png&#34;&gt;&lt;img src=&#34;https://k6.io/blog/static/c48fc5ca8336d9cd8b98618c4b9d86ec/7842b/grpc-unary.png&#34; alt=&#34;unary call&#34; title=&#34;unary call&#34; /&gt;&lt;/a&gt;&lt;/p&gt;

&lt;h3 id=&#34;server-streaming&#34;&gt;Server Streaming&lt;/h3&gt;

&lt;p&gt;In server streaming mode, the client sends a single request to the server, which in turn replies with multiple responses.&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;https://simme.dev/blog/static/fbc08c79e035da81aca99e4bc220f95e/7842b/grpc-server.png&#34;&gt;&lt;img src=&#34;https://k6.io/blog/static/fbc08c79e035da81aca99e4bc220f95e/7842b/grpc-server.png&#34; alt=&#34;server streaming&#34; title=&#34;server streaming&#34; /&gt;&lt;/a&gt;&lt;/p&gt;

&lt;h3 id=&#34;client-streaming&#34;&gt;Client Streaming&lt;/h3&gt;

&lt;p&gt;The client streaming mode is the opposite of the server streaming mode. The client sends multiple requests to the server, which in turn replies with a single response.&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;https://simme.dev/blog/static/ebdd5a0dedb7bdae80e59ceba69c6b75/7842b/grpc-client.png&#34;&gt;&lt;img src=&#34;https://k6.io/blog/static/ebdd5a0dedb7bdae80e59ceba69c6b75/7842b/grpc-client.png&#34; alt=&#34;client streaming&#34; title=&#34;client streaming&#34; /&gt;&lt;/a&gt;&lt;/p&gt;

&lt;h3 id=&#34;bi-directional-streaming&#34;&gt;Bi-directional streaming&lt;/h3&gt;

&lt;p&gt;In bi-directional streaming mode, both the client and the server may send multiple messages.&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;https://simme.dev/blog/static/094a57810030c8df50d05a8c87a1dee1/7842b/grpc-bidirectional.png&#34;&gt;&lt;img src=&#34;https://k6.io/blog/static/094a57810030c8df50d05a8c87a1dee1/7842b/grpc-bidirectional.png&#34; alt=&#34;bi-directional streaming&#34; title=&#34;bi-directional streaming&#34; /&gt;&lt;/a&gt;&lt;/p&gt;

&lt;h2 id=&#34;the-proto-definition&#34;&gt;The &lt;code&gt;.proto&lt;/code&gt; definition&lt;/h2&gt;

&lt;p&gt;The messages and services used for gRPC are described in .proto files, containing&lt;a href=&#34;https://en.wikipedia.org/wiki/Protocol_Buffers&#34;&gt;Protocol buffers&lt;/a&gt;, or protobuf, definitions.&lt;/p&gt;

&lt;p&gt;The definition file is then used to generate code which can be used by both senders and receivers as a contract for communicating through these messages and services. As the binary format used by gRPC lacks any self-describing properties, this is the only way for senders and receivers to know how to interpret the messages.&lt;/p&gt;

&lt;p&gt;Throughout this article, we&amp;rsquo;ll use the &lt;code&gt;hello.proto&lt;/code&gt; definition available for download on the&lt;a href=&#34;https://grpcbin.test.k6.io/&#34;&gt;k6 grpcbin website&lt;/a&gt;. For details on how to build your own grpc proto definition, see &lt;a href=&#34;https://grpc.io/docs/what-is-grpc/core-concepts/&#34;&gt;this excellent article from the official gRPC docs&lt;/a&gt;.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-proto&#34;&gt;// ./definitions/hello.proto

// based on https://grpc.io/docs/guides/concepts.html

syntax = &amp;quot;proto2&amp;quot;;

package hello;

service HelloService {
  rpc SayHello(HelloRequest) returns (HelloResponse);
  rpc LotsOfReplies(HelloRequest) returns (stream HelloResponse);
  rpc LotsOfGreetings(stream HelloRequest) returns (HelloResponse);
  rpc BidiHello(stream HelloRequest) returns (stream HelloResponse);
}

message HelloRequest {
  optional string greeting = 1;
}

message HelloResponse {
  required string reply = 1;
}

&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;getting-started&#34;&gt;Getting started&lt;/h2&gt;

&lt;p&gt;With k6 v0.29.0, we&amp;rsquo;re happy to introduce a native client for gRPC communication. In this early release, we&amp;rsquo;ve settled for providing a solid experience for unary calls. If any of the other modes would be particularly useful for you, we&amp;rsquo;d love to hear about your use case so we can move it up our backlog.&lt;/p&gt;

&lt;p&gt;The current API for working with gRPC in k6 using the native client is as follows:&lt;/p&gt;

&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Method&lt;/th&gt;
&lt;th&gt;Description&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;

&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;a href=&#34;https://k6.io/blog/javascript-api/k6-net-grpc/client/client-load-importpaths----protofiles&#34;&gt;Client.load(importPaths, &amp;hellip;protoFiles)&lt;/a&gt;&lt;/td&gt;
&lt;td&gt;Loads and parses the given protocol buffer definitions to be made available for RPC requests.&lt;/td&gt;
&lt;/tr&gt;

&lt;tr&gt;
&lt;td&gt;&lt;a href=&#34;https://k6.io/blog/javascript-api/k6-net-grpc/client/client-connect-address-params&#34;&gt;Client.connect(address [,params])&lt;/a&gt;&lt;/td&gt;
&lt;td&gt;Opens a connection to the given gRPC server.&lt;/td&gt;
&lt;/tr&gt;

&lt;tr&gt;
&lt;td&gt;&lt;a href=&#34;https://k6.io/blog/javascript-api/k6-net-grpc/client/client-invoke-url-request-params&#34;&gt;Client.invoke(url, request [,params])&lt;/a&gt;&lt;/td&gt;
&lt;td&gt;Makes an unary RPC for the given service/method and returns a &lt;a href=&#34;https://k6.io/blog/javascript-api/k6-net-grpc/response&#34;&gt;Response&lt;/a&gt;.&lt;/td&gt;
&lt;/tr&gt;

&lt;tr&gt;
&lt;td&gt;&lt;a href=&#34;https://k6.io/blog/javascript-api/k6-net-grpc/client/client-close&#34;&gt;Client.close()&lt;/a&gt;&lt;/td&gt;
&lt;td&gt;Close the connection to the gRPC service.&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;h3 id=&#34;creating-the-test&#34;&gt;Creating the test&lt;/h3&gt;

&lt;p&gt;The gRPC module is a separate package, available from your test script as &lt;code&gt;k6/net/grpc&lt;/code&gt;. Before we can use it, we first have to create an instance of the client. Instantiating the client, as well as the &lt;code&gt;.load&lt;/code&gt; operation, is only available during test initialization, ie. directly in the global scope.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;import grpc from &#39;k6/net/grpc&#39;;

const client = new grpc.Client();

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Next, we&amp;rsquo;ll load a &lt;code&gt;.proto&lt;/code&gt; definition applicable for the system under test. For the purpose of this article, we&amp;rsquo;ll use &lt;a href=&#34;https://grpcbin.test.k6.io/&#34;&gt;k6 grpcbin&lt;/a&gt;. Feel free to change this to whatever you please but keep in mind that you will also need an appropriate &lt;code&gt;.proto&lt;/code&gt; definition for the server you&amp;rsquo;re testing. The &lt;code&gt;.load()&lt;/code&gt; function takes two arguments, the first one being an array of paths to search for proto files, and the second being the name of the file to load.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;import grpc from &#39;k6/net/grpc&#39;;

const client = new grpc.Client();
client.load([&#39;definitions&#39;], &#39;hello.proto&#39;);

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Once that is done, we&amp;rsquo;ll go ahead and write our actual test.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;import grpc from &#39;k6/net/grpc&#39;;

const client = new grpc.Client();
client.load([&#39;definitions&#39;], &#39;hello.proto&#39;);

export default () =&amp;gt; {
  client.connect(&#39;grpcbin.test.k6.io:9001&#39;, {
    // plaintext: false
  });

  const data = { greeting: &#39;Bert&#39; };
  const response = client.invoke(&#39;hello.HelloService/SayHello&#39;, data);

  check(response, {
    &#39;status is OK&#39;: (r) =&amp;gt; r &amp;amp;&amp;amp; r.status === grpc.StatusOK,
  });

  console.log(JSON.stringify(response.message));

  client.close();
  sleep(1);
};

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;So let&amp;rsquo;s walk through this script to make sure we understand what&amp;rsquo;s happening. First, we use the&lt;code&gt;.connect()&lt;/code&gt; function to connect to our system under test. By default, the client will set &lt;code&gt;plaintext&lt;/code&gt; to false, only allowing you to use encrypted connections. If you, for any reason, need to connect to a server that lacks SSL/TLS, just flip this setting to &lt;code&gt;true&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;We then continue by creating the object we want to send to the remote procedure we&amp;rsquo;re invoking. In the case of &lt;code&gt;SayHello&lt;/code&gt;, it allows us to specify who the greeting should address using the &lt;code&gt;greeting&lt;/code&gt; parameter.&lt;/p&gt;

&lt;p&gt;Next, we invoke the remote procedure, using the syntax &lt;code&gt;&amp;lt;package&amp;gt;.&amp;lt;service&amp;gt;/&amp;lt;procedure&amp;gt;&lt;/code&gt;, as described in our proto file. This call is made synchronously, with a default timeout of 60000 ms (60 seconds). To change the timeout, add the key &lt;code&gt;timeout&lt;/code&gt; to the config object of &lt;code&gt;.connect()&lt;/code&gt; with the duration as the value, for instance &lt;code&gt;&#39;2s&#39;&lt;/code&gt; for 2 seconds.&lt;/p&gt;

&lt;p&gt;Once we&amp;rsquo;ve received a response from the server, we&amp;rsquo;ll then make sure the procedure executed successfully. The grpc module includes constants for this comparison which are listed &lt;a href=&#34;https://k6.io/docs/javascript-api/k6-net-grpc/constants&#34;&gt;here&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;Comparing the response status with &lt;code&gt;grpc.StatusOK&lt;/code&gt;, which is &lt;code&gt;200 OK&lt;/code&gt; just like for HTTP/1.1 communication, ensures the call was completed successfully.&lt;/p&gt;

&lt;p&gt;We&amp;rsquo;ll then log the message in the response, close the client connection, and sleep for a second.&lt;/p&gt;

&lt;h3 id=&#34;executing-the-test&#34;&gt;Executing the test&lt;/h3&gt;

&lt;p&gt;The test can be executed just like any other test, although you need to make sure you&amp;rsquo;re on at least version &lt;code&gt;v0.29.0&lt;/code&gt; to have access to the gRPC module. To check this, run the following command:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;$ k6 version
k6 v0.29.0 ((devel), go1.15.3, darwin/amd64)

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Anything less than &lt;code&gt;v0.29.0&lt;/code&gt; here, will require you to first update k6. Instructions on how to do that can be found &lt;a href=&#34;https://k6.io/docs/getting-started/installation&#34;&gt;here&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;Once that&amp;rsquo;s out of the way, let&amp;rsquo;s run our test:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ k6 run test.js

          /\      |‾‾| /‾‾/   /‾‾/
     /\  /  \     |  |/  /   /  /
    /  \/    \    |     (   /   ‾‾\
   /          \   |  |\  \ |  (‾)  |
  / __________ \  |__| \__\ \_____/ .io

  execution: local
     script: /Users/simme/code/grpc/test.js
     output: -

  scenarios: (100.00%) 1 scenario, 1 max VUs, 10m30s max duration (incl. graceful stop):
           * default: 1 iterations for each of 1 VUs (maxDuration: 10m0s, gracefulStop: 30s)

INFO[0000] {&amp;quot;reply&amp;quot;:&amp;quot;hello Bert&amp;quot;} source=console

running (00m01.4s), 0/1 VUs, 1 complete and 0 interrupted iterations
default ✓ [======================================] 1 VUs 00m01.4s/10m0s 1/1 iters, 1 per VU

    ✓ status is OK

    checks...............: 100.00% ✓ 1 ✗ 0
    data_received........: 3.0 kB 2.1 kB/s
    data_sent............: 731 B 522 B/s
    grpc_req_duration....: avg=48.44ms min=48.44ms med=48.44ms max=48.44ms p(90)=48.44ms p(95)=48.44ms
    iteration_duration...: avg=1.37s min=1.37s med=1.37s max=1.37s p(90)=1.37s p(95)=1.37s
    iterations...........: 1 0.714536/s
    vus..................: 1 min=1 max=1
    vus_max..............: 1 min=1 max=1

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;From the output, we can now tell that our script is working and that the server indeed responds with a greeting addressed to who, or what, we supplied in our request body. We can also see that our &lt;code&gt;check&lt;/code&gt; was successful, meaning the server responded with &lt;code&gt;200 OK&lt;/code&gt;.&lt;/p&gt;

&lt;h2 id=&#34;summary&#34;&gt;Summary&lt;/h2&gt;

&lt;p&gt;In this article, we&amp;rsquo;ve gone through some of the fundamentals of gRPC and how it works. We&amp;rsquo;ve also had a look at the gRPC client introduced in k6 v0.29.0. Last, but not least, we&amp;rsquo;ve created a working test script demonstrating this functionality.&lt;/p&gt;

&lt;p&gt;And that concludes this gRPC load testing tutorial. Thank you for reading!&lt;/p&gt;

&lt;h2 id=&#34;see-also&#34;&gt;See Also&lt;/h2&gt;

&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://k6.io/docs/javascript-api/k6-net-grpc&#34;&gt;k6 gRPC Module API&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://grpcbin.test.k6.io/&#34;&gt;k6 gRPCBin: A simple request/response service for gRPC&lt;/a&gt;, similar to &lt;a href=&#34;https://httpbin.test.k6.io/&#34;&gt;k6 httpbin&lt;/a&gt;.&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://grpc.io/&#34;&gt;The official website of the gRPC project&lt;/a&gt;.&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/grpc/grpc/tree/master/examples&#34;&gt;Official examples from the gRPC repo on GitHub&lt;/a&gt;.&lt;/li&gt;
&lt;/ul&gt;</description>
    </item>
    
    <item>
      <title>Performance testing with generated data</title>
      <link>https://simme.dev/posts/performance-testing-with-generated-data/</link>
      <pubDate>Tue, 17 Mar 2020 00:00:00 +0000</pubDate>
      <author>simme@arcticbit.se (Simon Aronsson)</author>
      <guid>https://simme.dev/posts/performance-testing-with-generated-data/</guid>
      <description>&lt;p&gt;While performance testing, it might not be a huge issue if the data you submit as part of your tests only vary slightly. In some cases, however, you might find yourself in a position where you&amp;rsquo;d like to keep not only the user interactions but also the data, as realistic as possible. How do we accomplish this without having to maintain long data tables? In this article, we&amp;rsquo;ll explore how we may utilize [fakerjs]() and k6 to perform load tests using realistic generated data.&lt;/p&gt;

&lt;h3 id=&#34;what-is-k6&#34;&gt;What is k6?&lt;/h3&gt;

&lt;p&gt;k6 is an open-source performance testing tool written and maintained by the team at &lt;a href=&#34;https://k6.io/&#34;&gt;k6&lt;/a&gt;. One of the main goals of the project is to provide users with a developer-centered, code-first approach to performance testing.&lt;/p&gt;

&lt;blockquote&gt;
&lt;h3 id=&#34;completely-new-to-k6&#34;&gt;🤓 Completely new to k6?&lt;/h3&gt;

&lt;p&gt;Then it might be a good idea to start out with this &lt;a href=&#34;https://dev.to/mostafa/beginner-s-guide-to-load-testing-with-k6-1od2&#34;&gt;Beginners guide to k6&lt;/a&gt;, written by &lt;a href=&#34;https://dev.to/mostafa&#34;&gt;Mostafa Moradian&lt;/a&gt;.&lt;/p&gt;
&lt;/blockquote&gt;

&lt;h3 id=&#34;what-is-faker&#34;&gt;What is Faker?&lt;/h3&gt;

&lt;p&gt;Faker is a tool used for generating realistic data. It&amp;rsquo;s available for a lot of different languages - &lt;a href=&#34;https://github.com/joke2k/faker&#34;&gt;python&lt;/a&gt;, &lt;a href=&#34;https://github.com/faker-ruby/faker&#34;&gt;ruby&lt;/a&gt;, &lt;a href=&#34;https://github.com/fzaninotto/Faker&#34;&gt;php&lt;/a&gt; and &lt;a href=&#34;https://github.com/DiUS/java-faker&#34;&gt;java&lt;/a&gt; to name a few.&lt;/p&gt;

&lt;p&gt;In this particular case, we&amp;rsquo;ll use the javascript implementation, fakerjs, as it allows us to use it from within our test script, rather than generating the data before execution.&lt;/p&gt;

&lt;h2 id=&#34;goals&#34;&gt;Goals&lt;/h2&gt;

&lt;p&gt;Historically performance testing, to a large extent, has been performed by running your test and then manually analyzing the result to spot performance degradation or deviations. k6 uses a different approach, utilizing goal-oriented performance thresholds to create pass/fail tollgates. Let&amp;rsquo;s formulate a scenario (or use case if you prefer) for this test and what it tries to measure.&lt;/p&gt;

&lt;h3 id=&#34;the-acme-corp-scenario&#34;&gt;The Acme Corp Scenario&lt;/h3&gt;

&lt;p&gt;Acme Corp is about to release a submission form, allowing users to sign up for their newsletter. As they plan to release this form during Black Friday, they want to make sure that it can withstand the pressure of a lot of simultaneous registrations. After all, they are a company in the business of making everything, so they expect a surge of traffic Friday morning.&lt;/p&gt;

&lt;h3 id=&#34;our-test-goals&#34;&gt;Our test goals&lt;/h3&gt;

&lt;p&gt;While we could very well set up complex custom thresholds, it&amp;rsquo;s usually more than enough to stick with the basics. In this case, we&amp;rsquo;ll measure the number of requests where we don&amp;rsquo;t receive an HTTP OK (200) status code in the response, as well as the total duration of each request.&lt;/p&gt;

&lt;p&gt;We&amp;rsquo;ll also perform the test with 300 virtual users, which will all perform these requests simultaneously.&lt;/p&gt;

&lt;h4 id=&#34;configuration&#34;&gt;Configuration&lt;/h4&gt;

&lt;p&gt;In k6, we express this as:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;const formFailRate = new Rate(&#39;failed form fetches&#39;);
const submitFailRate = new Rate(&#39;failed form submits&#39;);

export const options = {
  // ...
  vus: 300,
  thresholds: {
    &#39;failed form submits&#39;: [&#39;rate&amp;lt;0.1&#39;],
    &#39;failed form fetches&#39;: [&#39;rate&amp;lt;0.1&#39;],
    http_req_duration: [&#39;p(95)&amp;lt;400&#39;],
  },
};
&lt;/code&gt;&lt;/pre&gt;

&lt;h4 id=&#34;what-does-this-mean&#34;&gt;What does this mean?&lt;/h4&gt;

&lt;p&gt;So, let&amp;rsquo;s go through what we&amp;rsquo;ve done here. With 300 virtual users trying to fetch and submit the subscription form every second, we&amp;rsquo;ve set up the following performance goals:&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;Less than 10% are allowed to fail in retrieving the form&lt;/li&gt;
&lt;li&gt;Less than 10% are allowed to fail in submitting the form data&lt;/li&gt;
&lt;li&gt;Only 5% or less are permitted to have a request duration longer than 400ms&lt;/li&gt;
&lt;/ul&gt;

&lt;hr /&gt;

&lt;h2 id=&#34;the-actual-test&#34;&gt;The actual test&lt;/h2&gt;

&lt;p&gt;Now, let&amp;rsquo;s get on to the actual test code. The test code, which is executed by each VU once for each iteration, is put inside an anonymous function. We then expose this function as a default export.&lt;/p&gt;

&lt;h3 id=&#34;the-sleep-test&#34;&gt;The sleep test 😴&lt;/h3&gt;

&lt;p&gt;To make sure our environment is working, I usually start by setting up a test that does nothing except sleeping for a second and execute it once.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;import { sleep } from &#39;k6&#39;;

export default function () {
  sleep(1);
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Which, when run, produces output similar to this:&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://dev-to-uploads.s3.amazonaws.com/i/mck8bbbkeyhe659a7k4i.png&#34; alt=&#34;Running a k6 script with just sleep&#34; /&gt;&lt;/p&gt;

&lt;h3 id=&#34;adding-our-thresholds&#34;&gt;Adding our thresholds&lt;/h3&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;import { sleep } from &#39;k6&#39;;
import { Rate } from &#39;k6/metrics&#39;;

const formFailRate = new Rate(&#39;failed form fetches&#39;);
const submitFailRate = new Rate(&#39;failed form submits&#39;);

export const options = {
  // ...
  vus: 300,
  duration: &#39;10s&#39;,
  thresholds: {
    &#39;failed form submits&#39;: [&#39;rate&amp;lt;0.1&#39;],
    &#39;failed form fetches&#39;: [&#39;rate&amp;lt;0.1&#39;],
    http_req_duration: [&#39;p(95)&amp;lt;400&#39;],
  },
};

export default function () {
  formFailRate.add(0);
  submitFailRate.add(0);
  sleep(1);
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Notice the two new lines in the default function? For each iteration, we&amp;rsquo;re now adding data points to our &lt;a href=&#34;https://k6.io/docs/using-k6/thresholds&#34;&gt;threshold&lt;/a&gt; metrics, telling it that our requests did not fail. We&amp;rsquo;ll hook these up to do something meaningful as we proceed. We also added a duration to make the script run for more than one iteration.&lt;/p&gt;

&lt;p&gt;For now, running the script should give you the following output:&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://dev-to-uploads.s3.amazonaws.com/i/tofe0dhv86f4pcfesjnk.png&#34; alt=&#34;Running a k6 script with sleep and rates&#34; /&gt;&lt;/p&gt;

&lt;p&gt;Yay, it passes! Two green checks!&lt;/p&gt;

&lt;h3 id=&#34;adding-requests&#34;&gt;Adding requests&lt;/h3&gt;

&lt;p&gt;To be able to measure anything useful, we also need to add some actual requests. In this example, we&amp;rsquo;ll use &lt;a href=&#34;https://httpbin.test.loadimpact.com/&#34;&gt;https://httpbin.test.loadimpact.com/&lt;/a&gt; as our API, which is our mirror of the popular tool &lt;a href=&#34;https://httpbin.org/&#34;&gt;HTTPBin&lt;/a&gt;. Feel free to use whatever HTTP Request sink you prefer!&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;import { sleep } from &#39;k6&#39;;
import { Rate } from &#39;k6/metrics&#39;;
import http from &#39;k6/http&#39;;

const baseUrl = &#39;https://httpbin.test.loadimpact.com/anything&#39;;
const urls = {
  form: `${baseUrl}/form`,
  submit: `${baseUrl}/form/subscribe`,
};

const formFailRate = new Rate(&#39;failed form fetches&#39;);
const submitFailRate = new Rate(&#39;failed form submits&#39;);

export const options = {
  vus: 300,
  duration: &#39;10s&#39;,
  thresholds: {
    &#39;failed form submits&#39;: [&#39;rate&amp;lt;0.1&#39;],
    &#39;failed form fetches&#39;: [&#39;rate&amp;lt;0.1&#39;],
    http_req_duration: [&#39;p(95)&amp;lt;400&#39;],
  },
};

const getForm = () =&amp;gt; {
  const formResult = http.get(urls.form);
  formFailRate.add(formResult.status !== 200);
};

const submitForm = () =&amp;gt; {
  const submitResult = http.post(urls.submit, {});
  submitFailRate.add(submitResult.status !== 200);
};

export default function () {
  getForm();
  submitForm();
  sleep(1);
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;And once again:&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://dev-to-uploads.s3.amazonaws.com/i/zhtf4sdrgpqzrnxxq569.png&#34; alt=&#34;Running k6 with requests&#34; /&gt;&lt;/p&gt;

&lt;p&gt;The output now also includes metrics around our HTTP requests, as well as a little green check next to the duration.&lt;/p&gt;

&lt;h3 id=&#34;adding-bundling-and-transpiling&#34;&gt;Adding Bundling and Transpiling&lt;/h3&gt;

&lt;p&gt;Now that we&amp;rsquo;ve got our script to work, it&amp;rsquo;s almost time to add faker. Before we do that, we need to make sure that k6 can use the faker library.&lt;/p&gt;

&lt;p&gt;As k6 does not run in a NodeJS environment, but rather in a goja VM, it needs a little help. Thankfully, it&amp;rsquo;s not that complex. We&amp;rsquo;ll use webpack and babel to achieve this, but any bundler compatible with babel would likely work.&lt;/p&gt;

&lt;p&gt;Let&amp;rsquo;s start by initializing an npm package and add all the dependencies we&amp;rsquo;ll need:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;$ yarn init -y &amp;amp;&amp;amp; yarn add \
    @babel/core \
    @babel/preset-env \
    babel-loader \
    core-js \
    webpack \
    webpack-cli
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;We&amp;rsquo;ll then create our webpack config. The details of webpack and babel are outside the scope of this article, but there are plenty of great resources out there on how it works.&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;// webpack.config.js

module.exports = {
  mode: &#39;production&#39;,
  entry: &#39;./src/index.js&#39;,
  output: {
    path: __dirname + &#39;/dist&#39;,
    filename: &#39;test.[name].js&#39;,
    libraryTarget: &#39;commonjs&#39;,
  },
  module: {
    rules: [{ test: /\.js$/, use: &#39;babel-loader&#39; }],
  },
  stats: {
    colors: true,
  },
  target: &#39;web&#39;,
  externals: /k6(\/.*)?/,
  devtool: &#39;source-map&#39;,
};
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;and the &lt;code&gt;.babelrc&lt;/code&gt; file:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-json&#34;&gt;{
  &amp;quot;presets&amp;quot;: [
    [
      &amp;quot;@babel/preset-env&amp;quot;,
      {
        &amp;quot;useBuiltIns&amp;quot;: &amp;quot;usage&amp;quot;,
        &amp;quot;corejs&amp;quot;: 3
      }
    ]
  ]
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;We&amp;rsquo;ll also modify our package.json so that we can launch our tests using yarn:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-diff&#34;&gt;{
  &amp;quot;name&amp;quot;: &amp;quot;k6-faker&amp;quot;,
  &amp;quot;scripts&amp;quot;: {
+   &amp;quot;pretest&amp;quot;: &amp;quot;webpack&amp;quot;,
+   &amp;quot;test&amp;quot;: &amp;quot;k6 run ./dist/test.main.js&amp;quot;
  },
  ...
}
&lt;/code&gt;&lt;/pre&gt;

&lt;blockquote&gt;
&lt;h3 id=&#34;did-you-know&#34;&gt;🧠 Did you know?&lt;/h3&gt;

&lt;p&gt;Using &lt;code&gt;pre&lt;/code&gt; or &lt;code&gt;post&lt;/code&gt; at the beginning of a script name, results in that script running before/after the script you&amp;rsquo;re invoking. In this case, the &lt;code&gt;pretest&lt;/code&gt; script ensures that every time we run our test, webpack first creates a new, fresh bundle from the source code. - Sweet, huh? 👍🏻&lt;/p&gt;
&lt;/blockquote&gt;

&lt;hr /&gt;

&lt;h2 id=&#34;enter-faker&#34;&gt;Enter Faker!&lt;/h2&gt;

&lt;p&gt;Let&amp;rsquo;s get right into it then! The first step is to add faker to our dependencies:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;$ yarn add faker
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Faker has a quite extensive library of data that it&amp;rsquo;s able to generate, ranging from company details to catchphrases and profile pictures. While these are all handy to have, we&amp;rsquo;ll only use a tiny subset of what faker has to offer. Our object follows this structure:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-json&#34;&gt;{
  &amp;quot;name&amp;quot;: &amp;quot;jane doe&amp;quot;,
  &amp;quot;title&amp;quot;: &amp;quot;intergalactic empress&amp;quot;,
  &amp;quot;company&amp;quot;: &amp;quot;Worldeaters Inc&amp;quot;,
  &amp;quot;email&amp;quot;: &amp;quot;jane@doe.example&amp;quot;,
  &amp;quot;country&amp;quot;: &amp;quot;N/A&amp;quot;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;We&amp;rsquo;ll now go ahead and create a service that we may use to generate said persons:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;// subscriber.js

import * as faker from &#39;faker/locale/en_US&#39;;

export const generateSubscriber = () =&amp;gt; ({
  name: `SUBSCRIPTION_TEST - ${faker.name.firstName()} ${faker.name.lastName()}`,
  title: faker.name.jobTitle(),
  company: faker.company.companyName(),
  email: faker.internet.email(),
  country: faker.address.country(),
});
&lt;/code&gt;&lt;/pre&gt;

&lt;blockquote&gt;
&lt;h3 id=&#34;possible-performance-issues-ahead&#34;&gt;👿 Possible performance issues ahead!&lt;/h3&gt;

&lt;p&gt;All dependencies added tend to balloon the memory consumption to some extent, especially when they scale up to 300 concurrent instances. Because of this, it&amp;rsquo;s crucial that we only import the locale(s) we are using in our test case.&lt;/p&gt;

&lt;p&gt;While putting together the example repository for this article, I noticed that using faker adds about 2.3MB of memory per VU, which for 300 VUs resulted in a total memory footprint of around 1.5GB.&lt;/p&gt;

&lt;p&gt;You can read more about the javascript performance in k6 and how to tune it &lt;a href=&#34;https://k6.io/docs/using-k6/javascript-compatibility-mode&#34;&gt;here&lt;/a&gt;.&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;You might have noticed that we prepend the name of the generated user with &lt;code&gt;SUBSCRIPTION_TEST&lt;/code&gt;. Adding a unique identifier for your test data is just something I find convenient to be able to quickly filter out all dummy data I&amp;rsquo;ve created as part of a test. While optional, this is usually a good idea - especially if you test against an environment that you can&amp;rsquo;t easily prune.&lt;/p&gt;

&lt;hr /&gt;

&lt;h2 id=&#34;final-assembly&#34;&gt;Final assembly&lt;/h2&gt;

&lt;p&gt;Now, let&amp;rsquo;s put it all together!&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;// index.js

import { sleep } from &#39;k6&#39;;
import http from &#39;k6/http&#39;;
import { Rate } from &#39;k6/metrics&#39;;

import { generateSubscriber } from &#39;./subscriber&#39;;

const baseUrl = &#39;https://httpbin.test.loadimpact.com/anything&#39;;
const urls = {
  form: `${baseUrl}/form`,
  submit: `${baseUrl}/form/subscribe`,
};

const formFailRate = new Rate(&#39;failed form fetches&#39;);
const submitFailRate = new Rate(&#39;failed form submits&#39;);

export const options = {
  vus: 300,
  duration: &#39;10s&#39;,
  thresholds: {
    &#39;failed form submits&#39;: [&#39;rate&amp;lt;0.1&#39;],
    &#39;failed form fetches&#39;: [&#39;rate&amp;lt;0.1&#39;],
    http_req_duration: [&#39;p(95)&amp;lt;400&#39;],
  },
};

const getForm = () =&amp;gt; {
  const formResult = http.get(urls.form);
  formFailRate.add(formResult.status !== 200);
};

const submitForm = () =&amp;gt; {
  const person = generateSubscriber();
  const payload = JSON.stringify(person);

  const submitResult = http.post(urls.submit, payload);
  submitFailRate.add(submitResult.status !== 200);
};

export default function () {
  getForm();
  submitForm();
  sleep(1);
}
&lt;/code&gt;&lt;/pre&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;// subscriber.js

import * as faker from &#39;faker/locale/en_US&#39;;

export const generateSubscriber = () =&amp;gt; ({
  name: `SUBSCRIPTION_TEST - ${faker.name.firstName()} ${faker.name.lastName()}`,
  title: faker.name.jobTitle(),
  company: faker.company.companyName(),
  email: faker.internet.email(),
  country: faker.address.country(),
});
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;And with that, we&amp;rsquo;re ready to go:&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://dev-to-uploads.s3.amazonaws.com/i/g89wqlleq9867qw8ce7r.png&#34; alt=&#34;Running k6 with faker, thresholds and http requests&#34; /&gt;&lt;/p&gt;

&lt;hr /&gt;

&lt;h2 id=&#34;closing-thoughts&#34;&gt;Closing thoughts&lt;/h2&gt;

&lt;p&gt;While the flexibility you get by combining the javascript engine used in k6 with webpack and babel is near endless, it&amp;rsquo;s essential to keep track of the memory consumption and performance of the actual test. After all, getting false positives due to our load generator being out of resources is not particularly helpful.&lt;/p&gt;

&lt;p&gt;All the code from this article is available as an example repository on
&lt;a href=&#34;https://github.com/k6io/example-data-generation&#34;&gt;GitHub&lt;/a&gt;, which I try to keep up to date with new versions of k6 and faker.&lt;/p&gt;

&lt;p&gt;I&amp;rsquo;d love to hear your thoughts, so please hit me up with questions and comments in the field below. 👇🏼&lt;/p&gt;</description>
    </item>
    
    <item>
      <title>Practical DevOps #3: Shifting Left</title>
      <link>https://simme.dev/posts/practical-devops-shifting-left/</link>
      <pubDate>Tue, 14 Jan 2020 23:01:00 +0100</pubDate>
      <author>simme@arcticbit.se (Simon Aronsson)</author>
      <guid>https://simme.dev/posts/practical-devops-shifting-left/</guid>
      <description>

&lt;p&gt;One topic I always keep coming back to when it comes to DevOps is shift-left testing.&lt;/p&gt;

&lt;h2 id=&#34;what-is-shift-left-testing&#34;&gt;What is shift left testing?&lt;/h2&gt;

&lt;p&gt;The term was initially coined by Larry Smith in 2001 &lt;a href=&#34;https://web.archive.org/web/20140810171940/http://collaboration.cmc.ec.gc.ca/science/rpn/biblio/ddj/Website/articles/DDJ/2001/0109/0109e/0109e.htm&#34;&gt;in an article in  Dr. Dobbs Journal&lt;/a&gt; and refers to how we, by testing as early as possible, may deliver both faster and with higher quality.&lt;/p&gt;

&lt;p&gt;What if we instead of postponing testing until the sprint is over and we&amp;rsquo;ve delivered a new increment to QA were to test all the time; together?&lt;/p&gt;

&lt;hr /&gt;

&lt;h2 id=&#34;tests-are-expensive&#34;&gt;Tests are expensive&lt;/h2&gt;

&lt;p&gt;Many a manager has expressed their dissatisfaction with engineers spending too much time working on test automation. The usual reasoning is that test automation is expensive, and that Devs shouldn&amp;rsquo;t be doing the job of the QA team. The development team themselves might also reject test automation as too time-consuming, arguing that their time is better spent developing new features.&lt;/p&gt;

&lt;p&gt;And they&amp;rsquo;re not entirely wrong. Test automation &lt;strong&gt;IS&lt;/strong&gt; expensive. However, it&amp;rsquo;s not nearly as expensive as manual testing. There are a couple of reasons as to why this usually holds:&lt;/p&gt;

&lt;h3 id=&#34;lost-context&#34;&gt;Lost context&lt;/h3&gt;

&lt;p&gt;&lt;img src=&#34;https://simme.dev/images/forgot.gif&#34; alt=&#34;forgot&#34; /&gt;&lt;/p&gt;

&lt;p&gt;The developer(s) who implemented the feature has moved on to other tickets, which eliminates the possible benefit of still being &amp;ldquo;in that context&amp;rdquo; mentally as the testing is performed.&lt;/p&gt;

&lt;p&gt;This, in turn, means that they&amp;rsquo;ll have to stop whatever they&amp;rsquo;re doing and brush up on the details any time the one performing the manual testing requires assistance.&lt;/p&gt;

&lt;h3 id=&#34;limiting-flow&#34;&gt;Limiting flow&lt;/h3&gt;

&lt;p&gt;&lt;img src=&#34;https://simme.dev/images/this-is-fine.gif&#34; alt=&#34;this is fine&#34; /&gt;&lt;/p&gt;

&lt;p&gt;Building a feature, just to put it in the &lt;code&gt;ready for testing&lt;/code&gt; stack until it gets tested is no way to deliver features. What if testing is postponed? Creating a fix might very well be a week or two from now, as sprint scopes seldom change once the sprint has started. The same goes for actually retesting the fix.&lt;/p&gt;

&lt;p&gt;You probably wouldn&amp;rsquo;t wait for a burger joint that waits for the cashier to tell the kitchen that the burger&amp;rsquo;s still raw on one side. They could just flip it an additional time as they cook it to make sure it&amp;rsquo;s done before they pass it on. Your features are no different.&lt;/p&gt;

&lt;h3 id=&#34;assurance-decay&#34;&gt;Assurance decay&lt;/h3&gt;

&lt;p&gt;The only thing we&amp;rsquo;ll be able to guarantee is that it worked &lt;em&gt;at the exact time of testing&lt;/em&gt;. After that, all bets are off. One could argue that the assurance given by manual tests decays, or rots, with time. Every time we want to increase the assurance again, we&amp;rsquo;ll have to execute the tests again. Automated tests on the other hand, while sometimes requiring some additional development effort, may be executed on every commit or push, without any additional effort.&lt;/p&gt;

&lt;h2 id=&#34;when-to-tests&#34;&gt;When to tests&lt;/h2&gt;

&lt;p&gt;So, when should we test? Only during the development phase of each feature or ticket?&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://simme.dev/images/when-to-test.png&#34; alt=&#34;when to test&#34; /&gt;&lt;/p&gt;

&lt;p&gt;I&amp;rsquo;d like to argue that we should test all the time. From doing whiteboard simulations together with stakeholders or while designing our feature, all the way to chaos engineering experiments in production.&lt;/p&gt;

&lt;p&gt;With that said, it&amp;rsquo;s still important that we aim at testing something as early as possible and reasonable. Otherwise, we&amp;rsquo;ll risk passing the defects on to the next step in our development process, making the process of fixing them a lot more expensive.&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://simme.dev/images/cost-of-defects.png&#34; alt=&#34;when to test&#34; /&gt;&lt;/p&gt;

&lt;h2 id=&#34;every-tool-has-its-purpose&#34;&gt;Every tool has its purpose&lt;/h2&gt;

&lt;p&gt;It might sound like I&amp;rsquo;m proposing that we get rid of manual testing completely. That&amp;rsquo;s not the case. There&amp;rsquo;s still an extremely valid use case for manual testing: exploratory testing.&lt;/p&gt;

&lt;p&gt;While test automation is great for making sure that what we know, or assume, is indeed still valid, it&amp;rsquo;s very ill-suited for exploring new features, finding unintended ways in which they might break.&lt;/p&gt;

&lt;p&gt;So, if you ever find yourself writing a test script (as in instruction, not shell- or javascript), immediately stop what you&amp;rsquo;re doing, take a step back and get started on automating it instead!&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Behavior Driven Testing in Go Using Ginkgo</title>
      <link>https://simme.dev/posts/behavior-driven-testing-in-go-using-ginkgo/</link>
      <pubDate>Sat, 21 Dec 2019 21:54:26 +0100</pubDate>
      <author>simme@arcticbit.se (Simon Aronsson)</author>
      <guid>https://simme.dev/posts/behavior-driven-testing-in-go-using-ginkgo/</guid>
      <description>

&lt;h3 id=&#34;test-automation&#34;&gt;Test automation&lt;/h3&gt;

&lt;p&gt;Test automation is the practice of taking tests that traditionally have been executed manually and instead implement them programmatically. By making your tests part of the code you also allow yourself to run the tests continuously every time you make a change in the code while adding little to no additional cost.&lt;/p&gt;

&lt;p&gt;A high amount of relevant, high-quality and automated tests is one of the most important factors in maintaining high software quality while keeping the cost of change as low as possible.&lt;/p&gt;

&lt;h3 id=&#34;the-problem&#34;&gt;The problem&lt;/h3&gt;

&lt;p&gt;A common problem while practicing test-driven development, or writing unit tests at all for that matter, is that the tests tend to be very centered around the technical implementation.&lt;/p&gt;

&lt;p&gt;Example:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func TestApprovePurchaseOrder_ShouldCallTheApprovalService(t *t.Testing)
{
  service := &amp;amp;MockApprovalService{}
  order := &amp;amp;PurchaseOrder{
    ApprovalService: service,
    // ...
  }
  order.Approve()
  Expect(service.HasBeenCalled).To(Equal(true))
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;While ensuring that the code does what we expect it to, this approach has a couple of downsides.&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;&lt;p&gt;For someone not fluent in reading code, it might be hard to tell what this test helps ensure.&lt;/p&gt;&lt;/li&gt;

&lt;li&gt;&lt;p&gt;If someone were to refactor, or change, the code under test; they would be likely to have to spend time rewriting the test - even though the business behavior might not even have changed.&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h4 id=&#34;describing-tests-through-behavior&#34;&gt;Describing tests through behavior&lt;/h4&gt;

&lt;p&gt;This is where using ginkgo and practicing behavior-driven testing really makes a difference. Consider the following test:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;the function for approving a purchase order
should call the approval service
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Now let’s rewrite it into a BDD-style test:&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;given a purchase order
 when it&#39;s pending approval
  and we approve it
 then it should become approved once 
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;At least to me, this test title is a lot clearer. Bu using this format, a product stakeholder, even with no coding experience what so ever, will be able to understand what this test case proves and under what circumstances.&lt;/p&gt;

&lt;p&gt;Let&amp;rsquo;s go through each of the building blocks that make up this test description:&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Given&lt;/strong&gt;, a certain context or initial condition
&lt;strong&gt;When&lt;/strong&gt;, a certain state trigger the scenario
&lt;strong&gt;Then&lt;/strong&gt;, we expect one or more outcome&lt;/p&gt;

&lt;p&gt;This semi-formal form of writing requirements is fairly easy to understand, for developers and business stakeholders alike. By making sure all participants understand what they aim to describe, we have created a behavior specification, or behavior test, that leaves very little room for interpretation.&lt;/p&gt;

&lt;h4 id=&#34;a-possible-solution&#34;&gt;A possible solution&lt;/h4&gt;

&lt;p&gt;Let’s write the whole thing:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;  var _ = Describe(&amp;quot;a purchase order&amp;quot;, func() {
    When(&amp;quot;pending approval&amp;quot;, func() {
      When(&amp;quot;we approve it&amp;quot;, func() {
        It(&amp;quot;should become approved once we approve it&amp;quot;, func() {
          service := &amp;amp;MockApprovalService{}
          order := &amp;amp;PurchaseOrder{
            ApprovalService: service,
            // ...
          }
          Expect(order.IsApproved).ToNot(Equal(true))
          order.Approve()
          Expect(order.IsApproved).To(Equal(true))
        })
      })
    })
  })
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;And that was it! We&amp;rsquo;ve now written our first behavior-driven unit test using ginkgo! 👏🏼&lt;/p&gt;

&lt;hr /&gt;

&lt;h3 id=&#34;so-to-summarize&#34;&gt;So, to summarize:&lt;/h3&gt;

&lt;ul&gt;
&lt;li&gt;Using &lt;a href=&#34;https://github.com/onsi/ginkgo&#34;&gt;ginkgo&lt;/a&gt;, or &lt;a href=&#34;https://github.com/smartystreets/goconvey/&#34;&gt;goconvey&lt;/a&gt; if you prefer, allows you to easily formulate tests that are decoupled from the actual implementation.&lt;/li&gt;
&lt;li&gt;Using common written English to describe our tests allows us to generate test reports readable and understandable by anyone on the product team.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Thanks for reading! 🙏🏼&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>